# R 011 — Progress for f_010 audit closure

## Scope

Close all must-fix items from `f_010.md`:

1. GPU multigrid+wedge regression coverage.
2. Replace `np.resize` fallback in wedge resampling with deterministic geometric behavior.

## Implementation progress

### Completed code changes

1. `pydynamo/src/pydynamo/core/align.py`
   - added `_center_crop_or_pad(...)`;
   - replaced `np.resize` fallback in `_resample_wedge_mask_to_shape(...)` with center-aligned crop/pad;
   - retained bounded output via clipping to `[0,1]`.
2. `pydynamo/test/test_align.py`
   - added CUDA regression: multigrid + direct wedge mask;
   - added CUDA regression: multigrid + table fsampling + auto wedge apply;
   - added non-integer resample unit test for shape/bounds/no-resize-pattern baseline.
3. `p_011.md`
   - added detailed plan for this closure round.

### Documentation updates completed

1. Updated `r_010.md` with:
   - f_010 must-fix closure details,
   - real CPU/GPU/full-suite command lines and results,
   - final status updated to `Completed (PASS)`.

## Validation results

1. CPU focused:
   - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q pydynamo/test/test_align.py pydynamo/test/test_alignment_command.py`
   - `41 passed, 1 warning`
2. GPU focused:
   - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q pydynamo/test/test_align.py -k "multigrid and wedge and cuda"`
   - `2 passed, 25 deselected`
3. Full suite:
   - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q`
   - `73 passed, 1 warning`
4. Lint:
   - `No linter errors found` on touched files.

## Current status

Completed. f_010 audit requirements are closed in this round.
