# F 011 — Audit verification for p_011 / r_011
# F 011 — 对 p_011 / r_011 的审计核查结论

## Scope / 审计范围

- `p_011.md`
- `r_011.md`
- Claimed implementation/test artifacts:
  - `pydynamo/src/pydynamo/core/align.py`
  - `pydynamo/src/pydynamo/commands/alignment.py`
  - `pydynamo/test/test_align.py`
  - `pydynamo/test/test_alignment_command.py`
  - `r_010.md`

---

## Verification summary / 核验摘要

All key claims in `r_011.md` were verified against code and rerun tests.

`r_011.md` 的核心声明已通过源码核查与测试复现。

### Reproduced test evidence / 复现实证

1. CPU focused:
   - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q pydynamo/test/test_align.py pydynamo/test/test_alignment_command.py`
   - result: `41 passed, 1 warning`
2. GPU focused:
   - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q pydynamo/test/test_align.py -k "multigrid and wedge and cuda"`
   - result: `2 passed, 25 deselected`
3. Full suite:
   - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q`
   - result: `73 passed, 1 warning`
4. Lint on touched files:
   - `No linter errors found`.

---

## Must-fix closure status (from f_010) / f_010 必改项闭环状态

1. **GPU multigrid+wedge regression coverage**  
   - status: **CLOSED**
   - evidence: two CUDA regression tests added and passing.

2. **Replace risky `np.resize` fallback in wedge resampling**  
   - status: **CLOSED**
   - evidence: `_center_crop_or_pad(...)` introduced; `_resample_wedge_mask_to_shape(...)` no longer uses `np.resize`; non-integer transition test added.

---

## Required modifications / 是否仍需修改

### No mandatory modification required for 011 closure.
### 本轮 011 闭环无“必须修改项”。

The implementation and evidence satisfy the acceptance conditions defined in `f_010.md`.

当前实现与证据满足 `f_010.md` 定义的验收条件。

---

## Optional hardening (non-blocking) / 可选增强（非阻塞）

1. Add one command-level CUDA smoke test in `test_alignment_command.py` for `multigrid_levels=2 + apply_wedge_scoring=true`, if CI has stable GPU runners.
2. Add a tiny benchmark note in docs for the added resampling path to track runtime impact on large volumes.

1. 若 CI 有稳定 GPU 资源，可在 `test_alignment_command.py` 增补一条命令级 CUDA smoke。  
2. 可在文档加一条重采样路径的轻量性能记录，便于长期跟踪。

---

## Final verdict / 最终结论

**PASS** — `p_011.md` / `r_011.md` are consistent with actual code and reproducible evidence; f_010 requirements are closed.

**通过（PASS）** — `p_011.md` / `r_011.md` 与实际代码和可复现实证一致，f_010 要求已闭环。
