# Synthetic alignment example: general -> input -> output -> search -> device -> filter

# --- General ---
pixel_size: 4.284                                      # Angstrom per pixel for filter/origin conversion
log_level: info                                        # logging level: debug|info|warning|error
log_file: alignment.log                                # log output file; relative path defaults to YAML directory
error_log_file: alignment.error.log                    # error output file; relative path defaults to YAML directory

# --- Input ---
particles: ../synthetic_data/out_subtomograms/particles.star  # particle metadata input
subtomograms: ../synthetic_data/out_subtomograms              # subtomogram directory
reference: ../synthetic_data/emd_32820_bin4.mrc               # alignment reference volume
vll: null                                              # optional vll path for tbl workflows

# --- Output ---
output_table: ../synthetic_data/out_subtomograms/refined.star  # refined metadata output path
output_star: ../synthetic_data/out_subtomograms/refined.star   # explicit star output path

# --- Search ---
cone_step: 20.0                                        # tilt search step (degrees)
cone_range: [0, 180]                                   # tilt search range [min,max] (degrees)
inplane_step: 20.0                                     # inplane search step (degrees)
inplane_range: [0, 360]                                # inplane search range [min,max] (degrees)
shift_search: 2                                        # translational search radius (voxels)
multigrid_levels: 2                                    # coarse-to-fine levels

# --- Device ---
device: auto                                           # cpu|cuda|auto; auto uses all detected GPUs
device_id: null                                        # optional single GPU id override
gpu_ids: null                                          # optional explicit GPU id list; null means all detected GPUs

# --- Filter ---
lowpass: null                                          # optional lowpass cutoff in Angstrom, null disables
fmask: null                                            # optional Fourier mask path (reserved)
fmask_classification: null                             # optional classification mask path (reserved)
