# P 011 — Audit f_010 closure plan

## Input

- Audit file: `f_010.md`
- Prior artifacts: `plan_010.md`, `r_010.md`

## Objectives

1. Close missing GPU regression coverage for multigrid+wedge paths.
2. Replace risky `np.resize` fallback in wedge mask resampling with deterministic geometric handling.
3. Update evidence docs (`r_010.md`) with real command outputs and final status.
4. Record this round planning/execution in `p_011.md` / `r_011.md`.

## Detailed change plan

### A) Core code hardening (`pydynamo/src/pydynamo/core/align.py`)

1. Replace fallback behavior in `_resample_wedge_mask_to_shape(...)`:
   - remove `np.resize` usage;
   - use deterministic center-aligned geometric crop/pad after interpolation when needed.
2. Add helper:
   - `_center_crop_or_pad(vol, target_shape)` for deterministic geometry-preserving final shape alignment.
3. Keep bounded output contract:
   - clip to `[0, 1]`.

### B) Test coverage extension (`pydynamo/test/test_align.py`)

1. Add GPU regression test:
   - `multigrid_levels=2` + direct `wedge_mask` + `device="cuda"`.
2. Add GPU regression test:
   - `multigrid_levels=2` + `fsampling_mode="table"` + `wedge_apply_to="auto"` + `device="cuda"`.
3. Add unit test for non-integer/odd-even shape transition in wedge resampling:
   - exact output shape equals target shape;
   - values in `[0,1]`;
   - output should not match `np.resize` repetition pattern baseline.

### C) Documentation/evidence updates

1. Update `r_010.md`:
   - include must-fix completion evidence for GPU tests and resampling strategy;
   - include real commands and results;
   - ensure final status reflects closure.
2. Produce round report in `r_011.md` with:
   - changed files,
   - validation matrix results,
   - sign-off statement.

## Validation matrix to run

1. CPU focused:
   - `python -m pytest -q pydynamo/test/test_align.py pydynamo/test/test_alignment_command.py`
2. GPU focused:
   - `python -m pytest -q pydynamo/test/test_align.py -k "multigrid and wedge and cuda"`
3. Full suite:
   - `python -m pytest -q`
4. Lint check:
   - touched files must have no new lint errors.

## Acceptance criteria

1. Both f_010 must-fix items implemented in code.
2. Added GPU regression tests exist and pass on CUDA machine.
3. Resampling fallback no longer uses `np.resize`.
4. `r_010.md` contains real command/output evidence and final sign-off status.
5. No new linter errors in touched files.
