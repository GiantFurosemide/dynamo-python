# pydynamo 性能优化进度汇总（r_019）

基于 jg_019.md 与 p_019.md，本进度文档记录实施状态与测试结果。

---

## 实施状态

| 规划项 | 状态 | 修改文件 | 备注 |
|--------|------|----------|------|
| P0.1 GPU 网格计算迁移 | ✓ 完成 | `core/align.py` | 新增 `_euler_zxz_to_rotation_matrix_batch`，`_rotate_volume_torch_gpu_batch` 网格在 GPU 上计算 |
| P0.2 批量 FFT NCC | ✓ 完成 | `core/align.py` | 新增 `_ncc_volume_fft_torch_batch`，batch 分支用批量 NCC 替代 per-angle 循环 |
| P1.1 Classification GPU swap ref 并行 | ✓ 完成 | `commands/classification.py` | `_align_particle_task` 内 swap+nref>1+cuda 时 ThreadPoolExecutor 并行对齐到各 ref |
| P1.2 GPU 路径预取 | ✓ 完成 | `commands/alignment.py`, `commands/classification.py` | `_run_one`/`_align_particle_task` 支持 part_preloaded；multi-GPU 与 single-GPU 路径均增加 prefetch_queue_size 预取 |

---

## 测试结果

```
pytest test/test_align.py test/test_alignment_command.py test/test_classification.py -v
======================== 53 passed, 9 warnings in ~2.3s ========================
```

- test_align.py: 29 passed
- test_alignment_command.py: 15 passed
- test_classification.py: 9 passed

---

## 配置项

- `prefetch_queue_size`：alignment/classification 已有，GPU 路径现生效（默认 2）
- `gpu_angle_batch_size`：已有，batch 分支受益于 P0 优化

---

## 与 jg_019 的对应

- P0.1 对应 jg_019 卡点 2.1：GPU 网格 CPU 计算 → 已迁移到 GPU
- P0.2 对应 jg_019 卡点 2.2：GPU NCC 逐角度 → 已批量 FFT NCC
- P1.1 对应 jg_019 卡点 2.3：Classification GPU swap 串行 → 已 ref 级并行
- P1.2 对应 jg_019 卡点 2.4：GPU 路径无预取 → 已增加预取

---

## 审查反馈处理（f_019）

| 反馈项 | 状态 | 说明 |
|--------|------|------|
| F1.1 batch NCC 使用零均值 b 做 FFT | ✓ 已修复 | `fb = torch.fft.fftn(b, ...)` 替代 `ref_rot_batch.float()` |
| F2.1 角度转换迁移到 GPU | ✓ 已实施 | `torch.deg2rad(triplets_t)` 替代 `np.deg2rad` + `torch.as_tensor` |

---

以上为 r_019 进度汇总。
