# R 010 — Progress report for multigrid/wedge shape mismatch fix

## Goal

Resolve alignment broadcast mismatch:

- `operands could not be broadcast together with shapes (coarse) (fullres) (coarse)`

under `multigrid + wedge scoring`.

## Progress

### Completed

1. Implemented stage-aware wedge mask shape handling in core alignment:
   - Added wedge mask resampling utilities.
   - Added explicit shape mismatch guard in Fourier support application.
   - Applied stage-shape alignment in both CPU and GPU single-scale functions.
2. Wired multigrid coarse stage to use coarse wedge mask:
   - CPU multigrid path updated.
   - GPU multigrid path updated.
3. Improved alignment command diagnostics:
   - Added run-level shape context logging.
   - Added task failure logs with particle path and key context.
4. Added regression tests:
   - Core-level CPU test: multigrid + wedge mask.
   - Core-level CPU test: multigrid + fsampling table wedge.
   - Command-level smoke: multigrid + wedge scoring.
5. Closed audit must-fix (f_010) items:
   - Added GPU regression tests for:
     - `multigrid_levels=2 + wedge_mask + device=cuda`
     - `multigrid_levels=2 + fsampling_mode=table + wedge_apply_to=auto + device=cuda`
   - Replaced risky `np.resize` fallback in wedge resampling with deterministic center-aligned crop/pad.
   - Added non-integer scale resampling unit test for shape/bounds/no-resize-pattern baseline.

### Files changed

- `pydynamo/src/pydynamo/core/align.py`
- `pydynamo/src/pydynamo/commands/alignment.py`
- `pydynamo/test/test_align.py`
- `pydynamo/test/test_alignment_command.py`
- `plan_010.md`
- `r_010.md`

## Validation

1. Focused tests:
   - Command:
     - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q pydynamo/test/test_align.py pydynamo/test/test_alignment_command.py`
   - Result:
     - `41 passed, 1 warning`
   - Note:
     - warning is expected `roseman_local` approximation warning.
2. GPU focused tests:
   - Command:
     - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q pydynamo/test/test_align.py -k "multigrid and wedge and cuda"`
   - Result:
     - `2 passed, 25 deselected`
3. Full suite:
   - Command:
     - `/home/muwang/miniforge3/envs/pydynamo/bin/python -m pytest -q`
   - Result:
     - `73 passed, 1 warning`
4. Lint checks on touched files:
   - Result:
     - `No linter errors found`.

## Current status

Completed (PASS). Both f_010 must-fix items are closed with real CPU+GPU evidence, and no new linter errors were introduced.
